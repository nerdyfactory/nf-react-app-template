export enum MUIColorEnums {
  primary = 'primary',
  inherit = 'inherit',
  secondary = 'secondary',
  default = 'default',
}

export enum MUIButtonVariantEnums {
  contained = 'contained',
  outlined = 'outlined',
  text = 'text',
}
